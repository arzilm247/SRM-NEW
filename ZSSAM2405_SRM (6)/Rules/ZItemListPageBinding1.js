export default function ZItemListPageBinding1(context) {
    const bindingObject = context.getBindingObject();
    //const TRO = bindingObject.TransferOrder;
    //alert("bindingObject :" + JSON.stringify(bindingObject));

    // return clientAPI.executeAction('/ZSSAM2405_SRM/Actions/SRM/OData/CreateOnlineOData.action')
    //     .then(() => {
    //         if (TransferOrder && TransferOrder.trim() !== '') {
    //             return clientAPI.read(
    //                 '/ZSSAM2405_SRM/Services/OnlineAssetManager.service',
    //                 'TOHeaderSet', [],
    //                 "$filter=TransferOrder eq '" + TransferOrder + "'");
    //         } else {
    //             return clientAPI.read(
    //                 '/ZSSAM2405_SRM/Services/OnlineAssetManager.service',
    //                 'TOHeaderSet',
    //                 []
    //             );
    //         }
    //     })
    //     .then(result => {
    //         if (result && result.length > 0) {

    //         }

}
